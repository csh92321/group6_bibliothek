<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.care.root.mybatis.member.MemberMapper">	<!-- MemberDAO클래스와 연결 -->

	<resultMap id="member" type="com.care.root.member.dto.MemberDTO">
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="pwd" column="pwd"/>
		<result property="email" column="email" />
		<result property="phone" column="phone" />
		<result property="birth" column="birth" />
		<result property="addr" column="addr" />
		<result property="payment" column="payment" />
		<result property="ticket" column="ticket" />
		<result property="payDay" column="pay_day"/>
		<result property="savedate" column="savedate" />
		<result property="meetCnt" column="meet_cnt" />
		<result property="teamOne" column="team_one" />
		<result property="teamTwo" column="team_two" />
		<result property="sessionId" column="session_id" />
		<result property="limitTime" column="limit_time" />
	</resultMap>
	
	<resultMap id="checkIdPwd" type="com.care.root.member.dto.MemberDTO" >
		<id property="id" column="id"/>
		<result property="pwd" column="pwd" />
	</resultMap>

	<select id="userCheck" resultMap="checkIdPwd">
		select id,pwd from member where id=#{id}
	</select>
	
	<insert id="register">
		insert into member(id,name,pwd,email,phone,birth,addr) values(#{id},#{name},#{pwd},#{email},#{phone},#{birth},#{addr}) 
	</insert>

	<update id="keepLogin">
		update member set session_id=#{sessionId}, limit_time=#{limitDate} where id=#{id}
	</update>
	
	<select id="getUserSessionId" resultMap="member">
		select * from member where session_id=#{sessionId}
	</select>

	<select id="idCheck" parameterType="String" resultType="Integer">
		select count(id) from member where id=#{id}
	</select>
	
	<select id="emailCheck" parameterType="String" resultType="Integer">
		select count(email) from member where email=#{email}
	</select>
	
	<select id="phoneCheck" parameterType="String" resultType="Integer">
		select count(phone) from member where phone=#{phone}
	</select>
</mapper>